import * as React from 'react';
import styles from './Useropinion.module.scss';
import type { IUseropinionProps } from './IUseropinionProps';


export default class Useropinion extends React.Component<IUseropinionProps> {
  public render(): React.ReactElement<IUseropinionProps> {
    const {
    } = this.props;

    return (
      <section >
        
      </section>
    );
  }
}
